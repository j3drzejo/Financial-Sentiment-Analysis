pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.8.0
scikit-learn>=1.4.0
nltk>=3.8.1
tqdm>=4.66.0

# Deep learning — choose ONE of the two blocks below:
# ── CPU only (smaller, works everywhere) ──────────────────────────────────────
# torch>=2.2.0
# torchvision>=0.17.0
# ── GPU / MPS (Apple Silicon or NVIDIA CUDA) ─────────────────────────────────
torch>=2.2.0

# Hugging Face stack
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0

# Jupyter
jupyter>=1.0.0
ipywidgets>=8.1.0
